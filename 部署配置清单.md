# Amharic Learner 部署配置清单（不含密钥）

> 用途：按步骤填完下面的项，小程序即可从「能打开」到「AI 教练 + 发音评分全可用」。
> 本文件不含任何真实密钥，可安全提交。

## 已填项
- [x] 小程序 AppID → `project.config.json` 的 `appid`（值：`wx1b182a4e82a61d84`）
- [x] 微信云开发环境 id → `miniprogram/config.js` 的 `cloudEnv`（值：`cloud1-d4g658z1x5264f151`）
- [ ] Fidel 字体 fileID（可选）→ `miniprogram/config.js` 的 `fidelFontFileID`

## 步骤 B：创建云开发环境（在本机微信开发者工具操作）
1. 用微信开发者工具「导入项目」，目录选本仓库根目录（会自动识别 `miniprogram/` 与 `cloudfunctions/`）。
2. 顶部工具栏点「云开发」→ 开通 → 新建环境（付费套餐，个人最低约 ¥20/月）。
3. 记下环境 id（形如 `amharic-1a2b3c`），填入 `miniprogram/config.js`：
   ```js
   module.exports = {
     cloudEnv: 'amharic-1a2b3c',   // ← 替换这里
     fidelFontFileID: ''
   };
   ```

## 步骤 C：创建数据库集合（云开发控制台 → 数据库）
逐个新建，权限统一选「仅创建者可读写」：
- [ ] `progress`
- [ ] `ai_logs`
- [ ] `tts_cache`
- [ ] `users`
- [ ] `settings`
- [ ] `error_logs`

## 步骤 D：云函数环境变量（云开发控制台 → 云函数 `api` → 配置 → 环境变量）
> 注意：这些是**独立服务**的密钥，不是小程序 AppSecret。

| 变量名 | 取值来源 | 必填 | 说明 |
|--------|----------|------|------|
| `DEEPSEEK_API_KEY` | platform.deepseek.com 申请 | 是 | AI 教练诊断/计划调整 |
| `DEEPSEEK_MODEL` | 可选，默认 `deepseek-chat` | 否 | 模型名 |
| `AZURE_SPEECH_KEY` | portal.azure.com 建 Speech 资源 | 是 | 阿姆哈拉语朗读与发音评分 |
| `AZURE_SPEECH_REGION` | 如 `southeastasia` | 是 | 与上面 Speech 资源同区域 |
| `ADMIN_OPENIDS` | 首次运行后「我的」页复制 | 否 | 管理员 openid，逗号分隔 |
| `AI_DAILY_LIMIT` | 可选，默认 20 | 否 | 每人每日 AI 调用上限 |
| `TTS_DAILY_LIMIT` | 可选，默认 300 | 否 | 每日 TTS 缓存未命中上限 |
| `STT_DAILY_LIMIT` | 可选，默认 100 | 否 | 每日发音评分上限 |
| `TTS_MONTHLY_CHARS_LIMIT` | 可选，默认 400000 | 否 | 全体本月语音字符上限 |

Azure Speech 建议用 **F0 免费层**（每月 50 万字符合成、5 小时识别），区域选 `southeastasia` 或 `eastasia`。

## 步骤 E：部署与分发
1. 右键 `cloudfunctions/api` → 「上传并部署：云端安装依赖」。
2. 开发者工具「上传」代码 → mp.weixin.qq.com「版本管理」设为体验版。
3. 「成员管理」添加同事微信号为体验成员（个人主体上限 15 人）。

## 功能可用性对照
| 功能 | 仅填 AppID | 填完 B/C | 填完 D |
|------|-----------|----------|--------|
| 课程 / 闪卡 / Fidel 认读 | ✅ | ✅ | ✅ |
| 进度云端同步 | ❌ | ✅ | ✅ |
| AI 教练诊断/调整 | ❌ | ❌ | ✅ |
| Azure 朗读 / 跟读发音评分 | ❌ | ❌ | ✅ |

## 安全提醒
- 小程序 **AppSecret 不需要**本项目（已 grep 确认 0 处引用），请只在微信公众平台保管，切勿写入代码或提交。
- 所有密钥只存在于云函数环境变量，不要硬编码进 `miniprogram/` 或 `cloudfunctions/` 源码。
